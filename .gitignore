# 의존성·빌드 산출물
node_modules/
dist/
*.tsbuildinfo
.yarn/cache/
.yarn/install-state.gz
.yarn/unplugged/

# 환경 변수 (Notion 토큰 등)
.env

# 에디터·세션 부산물
.omc/
*.local

# 하네스 런타임 상태 (페이즈마다 변동 — 추적 제외; merge 체크아웃 충돌 방지)
harness/state.json

# 드라이버 단일 실행 잠금 (loop.mjs 가 획득·해제하는 런타임 파일)
harness/state.lock

# 하네스 사이클 로그(페이즈마다 변동 — merge 체크아웃 충돌 방지, state.json 과 동일 성격)
harness/cycles/cycle-log.ndjson

# 결정적 게이트 실측 결과(페이즈마다 변동 — state.json 과 동일 성격. 사이클 스탬프로 신선도를 보므로
# 커밋할 이유가 없고, 커밋하면 merge 체크아웃 때 충돌한다)
harness/gate-status.json
