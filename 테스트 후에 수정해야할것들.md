# 테스트 후에 수정해야 할 것들

> expense-tracker(2차) 자율 테스트 중 발견. A=하네스 버그(코드/문서), B=시뮬레이션 품질/프로세스.

## 진행 상태 (branch `step/21-eval-visual-consume`)
- ✅ **B3 (캡처물 소비 평가)** — 구현 완료: `eval-playwright.mjs` 가 `dom.html`+`screenshot-mobile.png` 추가 캡처(스모크 확인), `run-cycle.md`/`eval-rubric.md §6`/`.claude/agents/{customer,ux,ui}.md` 에 "에이전트가 스크린샷·DOM 을 직접 보고 평가" 의무화.
- ◑ **B1 (UI 품질)** — `ui.md` 에 앱 셸/사이드 padding/반응형/자기점검 의무 추가(다음 사이클부터 강제). expense-tracker 기존 UI 재작업은 별도.
- ◑ **B2 (ux 미사용)** — `ux.md`/`run-cycle.md evaluate` 에 ux 실제 호출+캡처 소비 의무(다음 사이클부터). CEO 로스터에 ux 포함은 운영 규칙.
- ⬜ A1·A2·A3·A4 미착수.

## A. 하네스 버그 — 재발 방지 (코드·문서)

### A1. 프로젝트 설명 밑 "진행 중 · 사이클 0/2" 상태줄이 2개 (사용자 지적)
- 원인: 허브 콜아웃은 `callout.rich_text`(설명) + **자식 paragraph(상태줄)** 구조인데, §1b 가 상태줄을 rich_text 에 넣고 **옛 자식 paragraph(SCMS 사이클 5/5…)는 안 지움** → 중복 + 옛 데이터 잔존.
- Fix: §1b/콜아웃 갱신을 "rich_text=설명만, 상태줄=자식 paragraph 1개 update(없으면 생성, 추가 자식 삭제)"로. 초기화 상태줄은 중립(사이클 0/N). `docs/notion-hub-layout.md` §2 + `/start-project` §1b 명시.

### A2. 🔄 진행 상황 기본 뷰에 `평가` 컬럼 없음
- 원인: 기본 뷰 displayProperties 에 `평가` 누락(`한눈에` 뷰에만). 데이터는 정상.
- Fix: `docs/notion-hub-layout.md` §4 기본 뷰에 `평가` 포함. (라이브 템플릿은 `notion-update-view SHOW …,"평가",…` 로 이미 반영)

### A3. eval-playwright teardown 간헐 실패 (orphan dev 서버)
- 원인: `scripts/lib/teardown.mjs` 의 taskkill /T 가 자식 트리를 못 잡는 레이스(간헐). 포트 8000 orphan 잔존 → 후속 평가 strictPort 실패 위험.
- Fix: 포트 기준 kill 폴백(점유 PID 재조회 후 kill) + 종료 확인 재시도 루프.

### A4. merge 체크아웃이 cycle-log(추적) 로컬 변경으로 실패
- 원인: evaluate→debate→merge 연속 시 loop 가 그 사이 `cycle-log.ndjson` append → `git checkout main` 충돌(state.json 과 동류).
- Fix: (a) `harness/cycles/cycle-log.ndjson` 도 `.gitignore`(runtime 로그) **권장**, 또는 (b) merge-step 이 checkout 전 harness 런타임 파일 commit/stash. run-cycle.md §3 가이드는 "debate 와 merge 사이"에 커밋 강조(연속 호출 금지).

## B. 시뮬레이션 품질/프로세스 — 핵심 지적 (사용자 #2·#3)

### B1. UI 작업이 너무 형식적 (사용자 지적)
- 증상: 콘텐츠가 좌측 끝에 딱 붙음(사이드 padding·컨테이너·max-width 없음), 앱 셸 없음. ui 산출이 "컴포넌트 배치"에 그침.
- 원인: 구현을 generic executor 에 위임하며 레이아웃/시각 품질을 요구 안 함. 전용 `ui`/`designer` 에이전트의 디자인 패스 부재.
- Fix: (1) 앱 셸/레이아웃(중앙 `Container` + padding + 반응형) 추가. (2) UI 사이클은 `ui`(또는 `designer`) 에이전트가 **시각 품질·간격·정렬·상태(빈/에러/로딩)** 까지 실제로 책임. 구현 프롬프트에 레이아웃·a11y·시각 기준 명시.

### B2. UX 에이전트 미사용 — 협의체가 라벨뿐 (사용자 지적)
- 증상: Notion 에이전트 칸에 ux 가 없었고, evaluate 가 Playwright 루브릭+customer 뿐 → **UX 평가 자체가 빠짐.** 협의체(architect/ui/ux)가 실제 에이전트 호출이 아니라 오케스트레이터가 쓴 문서에 가까웠음.
- 원인: "린 로스터"로 축소하며 ux/designer 를 실제 호출 안 함. CEO subset 선정이 형식적.
- Fix: UI/UX 가 걸린 사이클은 CEO 로스터에 **ux 필수 포함** + decompose/evaluate/debate 에서 **ux 에이전트를 실제 호출**해 사용 흐름·정보구조·인터랙션 평가(`harness/evaluations/ux-<id>.md`). 순수 기능 수정만 ux 생략 가능. run-cycle.md "복잡 화면" 판정 시 다중 페르소나+ux 강제.

### B3. ⭐ 평가가 캡처물(스크린샷+DOM)을 실제로 소비하지 않음 — UI/UX 평가가 "블라인드 루브릭" (사용자 지적, B1·B2 의 근본 원인)
- 증상: `eval-playwright.mjs` 가 `screenshot.png` 는 저장하지만 **그 이미지를 어떤 에이전트도 보지 않음.** 채점은 자동 루브릭(title/heading/console/a11y 카운트)뿐 → "사이드 padding 없음·좌측에 딱 붙음" 같은 시각/레이아웃/UX 결함을 구조적으로 못 잡음(이번 UI 가 96으로 통과한 근본 원인). **DOM/HTML 덤프 로그도 없음.**
- 원인: evaluate 가 결정적 루브릭 단독. ui/ux/customer 에이전트가 캡처물을 **입력으로 받아 보고/읽고 추론하는 단계가 부재.** (오케스트레이터 육안 확인 ≠ 구조화된 에이전트 평가)
- Fix:
  1. evaluate 에서 **스크린샷 + DOM/HTML 덤프(+가능하면 데스크톱/모바일 폭 둘 다)를 로그로 저장**(`harness/evaluations/<id>/`).
  2. **ui·ux·customer 에이전트가 그 스크린샷(이미지)·DOM 을 실제로 보고/읽고** 레이아웃·간격·정렬·시각 품질·사용 흐름·a11y 를 평가 → 그 결과를 점수/이슈로 반영. **루브릭은 하한(앱이 떴는지)일 뿐, 시각·UX 판정은 캡처물을 본 에이전트가 한다.**
  3. 이게 되면 B1(패딩 없는 UI)·B2(ux 미평가)가 자동으로 잡힘 — "캡처 → 에이전트가 보고 평가" 루프가 핵심.
- `docs/eval-rubric.md` + `run-cycle.md` evaluate 절 + `.claude/agents/{customer,ux,ui}.md` 에 "캡처물 소비 평가" 의무화.

---
참고: A1 은 사용자 작성 #1, B1=#2, B2=#3, B3=캡처물 소비(사용자 추가 지적). A2~A4 는 오케스트레이터가 2차 테스트 중 발견해 합침.
